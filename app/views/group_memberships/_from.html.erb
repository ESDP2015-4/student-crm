

<% @students = @users.all_students_except(@group.student_ids) %>

<%= simple_form_for([@course, @group, @gm]) do |f| %>


    <%= f.button :submit, 'Добавить студентов' %>

    <%= f.association :student, collection: @students,label_method: :full_name, as: :check_boxes %>


<% end %>